using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Game.Enums  // ← 自定义你的命名空间名
{
    public enum DoorType
    {
        Red,
        Yellow,
        Blue
    }
    public enum GemType
    {
        Red, Blue
    }
}